import Foundation

public enum CloudSyncRecoveryCopy {
    public static let legacyIdentityUnavailable =
        "Legacy backup: key identity is unavailable. Enter the recovery key originally used when this backup was created."
}
